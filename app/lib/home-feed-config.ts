/** Días que carga el servidor en la home (hoy + mañana, estilo futbolhoy). */
export const HOME_SSR_DAY_COUNT = 2;

/** Tope por bloque cuando hay finales, eliminatorias o torneos top. */
export const HOME_SECTION_MAX_IMPORTANT = 5;

/** Tope por bloque en competiciones / deportes rutinarios. */
export const HOME_SECTION_MAX_DEFAULT = 2;

/** Tope total de tarjetas por día en vista destacada. */
export const HOME_DAILY_EVENT_CAP = 20;
